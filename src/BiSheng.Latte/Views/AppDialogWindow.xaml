<Window
    x:Class="BiSheng.Latte.Views.AppDialogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title=""
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">

    <Border
        Margin="20"
        Background="{DynamicResource Brush.Surface}"
        BorderBrush="{DynamicResource Brush.Border}"
        BorderThickness="1"
        CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="24"
                Opacity="0.16"
                ShadowDepth="0"
                Color="#000000" />
        </Border.Effect>

        <Grid MinWidth="400" MaxWidth="480">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题区：左侧强调条 + 图标 + 标题 -->
            <Grid Grid.Row="0">
                <Border
                    x:Name="AccentBar"
                    Width="4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Stretch"
                    Background="{DynamicResource Brush.Accent}"
                    CornerRadius="12,0,0,0" />

                <StackPanel Margin="24,22,24,0" Orientation="Horizontal">
                    <Border
                        x:Name="IconBadge"
                        Width="42"
                        Height="42"
                        Background="{DynamicResource Brush.Selected}"
                        CornerRadius="21">
                        <TextBlock
                            x:Name="IconText"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource UIFont}"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource Brush.Accent}"
                            Text="i" />
                    </Border>
                    <TextBlock
                        x:Name="TitleText"
                        Margin="14,0,0,0"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource UIFont}"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource Brush.Text}"
                        TextWrapping="Wrap" />
                </StackPanel>
            </Grid>

            <!-- 正文 -->
            <TextBlock
                x:Name="MessageText"
                Grid.Row="1"
                Margin="24,16,24,0"
                FontFamily="{StaticResource UIFont}"
                FontSize="14"
                Foreground="{DynamicResource Brush.TextSecondary}"
                LineHeight="22"
                TextWrapping="Wrap" />

            <!-- 按钮栏 -->
            <Border
                Grid.Row="2"
                Margin="0,22,0,0"
                Padding="24,14"
                Background="{DynamicResource Brush.SurfaceAlt}"
                BorderBrush="{DynamicResource Brush.BorderLight}"
                BorderThickness="0,1,0,0"
                CornerRadius="0,0,12,12">
                <StackPanel
                    x:Name="ButtonPanel"
                    Margin="-8,0,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal" />
            </Border>
        </Grid>
    </Border>
</Window>
